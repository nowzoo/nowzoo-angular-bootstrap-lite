import { PermalinkedDirective } from './permalinked.directive';

describe('PermalinkedDirective', () => {
  it('should create an instance', () => {
    const directive = new PermalinkedDirective();
    expect(directive).toBeTruthy();
  });
});
