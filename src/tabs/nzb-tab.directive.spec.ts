import { NzbTabDirective } from './nzb-tab.directive';

describe('NzbTabDirective', () => {
  it('should create an instance', () => {
    const directive = new NzbTabDirective();
    expect(directive).toBeTruthy();
  });
});
