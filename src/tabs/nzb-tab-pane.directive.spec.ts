import { NzbTabPaneDirective } from './nzb-tab-pane.directive';

describe('NzbTabPaneDirective', () => {
  it('should create an instance', () => {
    const directive = new NzbTabPaneDirective();
    expect(directive).toBeTruthy();
  });
});
