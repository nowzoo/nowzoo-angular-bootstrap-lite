import { NzbDropdownDirective } from './nzb-dropdown.directive';

describe('NzbDropdownDirective', () => {
  it('should create an instance', () => {
    const directive = new NzbDropdownDirective();
    expect(directive).toBeTruthy();
  });
});
