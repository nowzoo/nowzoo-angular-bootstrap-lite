import { NzbAlertDirective } from './nzb-alert.directive';

describe('NzbAlertDirective', () => {
  it('should create an instance', () => {
    const directive = new NzbAlertDirective();
    expect(directive).toBeTruthy();
  });
});
