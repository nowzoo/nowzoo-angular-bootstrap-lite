import { NzbModalDirective } from './nzb-modal.directive';

describe('NzbModalDirective', () => {
  it('should create an instance', () => {
    const directive = new NzbModalDirective();
    expect(directive).toBeTruthy();
  });
});
