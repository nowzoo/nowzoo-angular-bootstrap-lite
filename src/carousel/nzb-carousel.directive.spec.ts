import { NzbCarouselDirective } from './nzb-carousel.directive';

describe('NzbCarouselDirective', () => {
  it('should create an instance', () => {
    const directive = new NzbCarouselDirective();
    expect(directive).toBeTruthy();
  });
});
