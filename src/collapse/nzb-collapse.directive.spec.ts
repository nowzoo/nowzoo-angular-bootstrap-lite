import { NzbCollapseDirective } from './nzb-collapse.directive';

describe('NzbCollapseDirective', () => {
  it('should create an instance', () => {
    const directive = new NzbCollapseDirective();
    expect(directive).toBeTruthy();
  });
});
